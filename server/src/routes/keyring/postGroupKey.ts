import { RequestHandler } from "express";
import { reqWithUserinfo } from "../../types/reqWithUserinfo";

const postGroupKey: RequestHandler = (req: reqWithUserinfo, res) => {
    
}

export default postGroupKey;